<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GlpiMobile"
        tools:targetApi="31">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.LoginActivity"
            android:exported="false" />

        <activity
            android:name=".ui.TicketDetailActivity"
            android:exported="false" />

        <activity
            android:name=".ui.RespondTicketActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- NOVO: Inventário -->
        <activity
            android:name=".ui.InventoryActivity"
            android:exported="false"
            android:label="Inventário" />

        <!-- NOVO: Adicionar dispositivo -->
        <activity
            android:name=".ui.AddDeviceActivity"
            android:exported="false"
            android:label="Novo Dispositivo"
            android:windowSoftInputMode="adjustResize" />

        <!-- NOVO: Usuários -->
        <activity
            android:name=".ui.UsersActivity"
            android:exported="false"
            android:label="Usuários TecnoIT" />

    </application>

</manifest>
